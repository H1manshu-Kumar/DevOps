name: first github action

on:
  push:
    branches: [ main ]
    workflow_dispatch:

jobs:
  PrintHelloWorld:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello-World"

